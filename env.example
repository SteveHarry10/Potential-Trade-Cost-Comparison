# HTTPS endpoint that returns Floorplans rows as JSON. Keep the real value private.
SHAREPOINT_EXCEL_ENDPOINT=https://your-secure-flow-or-proxy.example.com/floorplans

# Optional when your endpoint expects a bearer token.
SHAREPOINT_EXCEL_TOKEN=
